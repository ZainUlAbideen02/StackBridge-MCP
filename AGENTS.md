# AGENTS.md — StackBridge AI Agent Architecture Guide

> Automated full-stack AST boundary map generated by **StackBridge-MCP**.

## 1. System Topology Overview
- **Total Dependency Nodes:** 15
- **Total Cross-Boundary Edges:** 7
- **Frontend Client Calls:** 4
- **Backend API Routes & MCP Tools:** 9
- **Database ORM Models:** 2

## 2. Full-Stack Boundary & Route Matrix

| Type | Endpoint / Target | Methods | Source File | Handler / Symbol | Referenced Models |
| :--- | :--- | :--- | :--- | :--- | :--- |
| MCP Tool | `tools/trace_fullstack_path` | `MCP_TOOL` | `stackbridge/mcp_server/server.py` | `trace_fullstack_path()` | — |
| MCP Tool | `tools/get_route_contract` | `MCP_TOOL` | `stackbridge/mcp_server/server.py` | `get_route_contract()` | — |
| MCP Tool | `tools/verify_schema_change` | `MCP_TOOL` | `stackbridge/mcp_server/server.py` | `verify_schema_change()` | — |
| MCP Tool | `tools/verify_breakage` | `MCP_TOOL` | `stackbridge/mcp_server/server.py` | `verify_breakage()` | — |
| MCP Tool | `tools/get_stack_health` | `MCP_TOOL` | `stackbridge/mcp_server/server.py` | `get_stack_health()` | — |
| FastAPI Route | `/api/v2/analytics/{org_id}` | `GET` | `tests/fixtures/advanced_fullstack/backend/routers/analytics.py` | `get_analytics()` | — |
| FastAPI Route | `/api/v2/auth/login` | `POST` | `tests/fixtures/advanced_fullstack/backend/routers/auth.py` | `login()` | — |
| FastAPI Route | `/api/v1/teams` | `GET` | `tests/fixtures/synthetic_fullstack/backend/routes.py` | `get_teams()` | — |
| FastAPI Route | `/api/v1/users/{user_id}/billing` | `GET` | `tests/fixtures/synthetic_fullstack/backend/routes.py` | `get_user_billing()` | — |

## 3. Database Schema & ORM Models

| Model Name | Table Name | Source File | Fields |
| :--- | :--- | :--- | :--- |
| `User` | `users` | `tests/fixtures/synthetic_fullstack/backend/models.py` | `id, email, name` |
| `BillingAccount` | `billing_accounts` | `tests/fixtures/synthetic_fullstack/backend/models.py` | `id, user_id, plan, balance` |

## 4. MCP Server Tools for Coding Agents

StackBridge exposes high-ergonomics JSON-RPC 2.0 MCP tools for AI agents:
- `trace_fullstack_path(symbol_or_path)`: Trace complete dependency graph across UI, routes, and DB.
- `get_route_contract(route_path, method)`: Extract complete request/response contracts and status codes.
- `verify_schema_change(modified_files)`: Run zero-false-positive AST breakage verification before edits.
- `verify_breakage(modified_files)`: Diagnostic compiler checks with formatted agent recommendations.
- `get_stack_health()`: Real-time fullstack error and drift statistics.

## 5. Agent Safety Guidelines & Guard Rules

1. **Never edit an ORM model field** without checking upstream FastAPI response models and TypeScript fetch callers.
2. **Run the pre-commit guard** before committing any changes:
   ```bash
   python -m stackbridge.main guard --repo-path .
   ```
3. **Predict test impact** before executing test suites to isolate impacted test callers.
